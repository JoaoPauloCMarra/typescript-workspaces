{
  "name": "server",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "nodemon --ext \"ts,json\" -r dotenv/config --config ./nodemon.prod.json",
    "dev": "nodemon --ext \"ts,json\" -r dotenv/config --config ./nodemon.dev.json",
    "lint": "tsc --noEmit -p ./tsconfig.json && eslint ./src --fix",
    "build": "tsc -p ./tsconfig.json"
  }
}
